// .toFixed(n) converts a number to a string with exactly n digits after the decimal point.
let num = 3.14159;
let fixedNum = num.toFixed(2); // keeps 2 decimal places
console.log(fixedNum);          // "3.14" (string)
console.log(typeof fixedNum);   // "string"
